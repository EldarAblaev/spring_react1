spring:
  profiles:
    active: @spring.profiles.active@

app:
  datasource:
    jdbc-url: jdbc:postgresql://localhost:5432/spring_react1
    username: postgres
    password: password
    pool-size: 30
